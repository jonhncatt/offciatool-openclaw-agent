OPENAI_API_KEY=<YOUR_OPENAI_API_KEY>

# If using a company OpenAI-compatible gateway
# OFFICETOOL_OPENAI_BASE_URL=https://<YOUR_COMPANY_API_BASE>/v1

# If your company gateway uses an internal root CA (equivalent to curl --cacert)
# OFFICETOOL_CA_CERT_PATH=/absolute/path/to/KIOXIAInternalRootCA.cer

# Most enterprise gateways are better with Chat Completions / tools semantics
# OFFICETOOL_USE_RESPONSES_API=false

# Optional settings
# OFFICETOOL_DEFAULT_MODEL=gpt-5.1-chat
# OFFICETOOL_SUMMARY_MODEL=gpt-5.1-chat
# OFFICETOOL_SUMMARY_MODE=gpt-5.1-chat
# OFFICETOOL_TEMPERATURE=
# OFFICETOOL_SUMMARY_TRIGGER_TURNS=2000
# OFFICETOOL_MAX_CONTEXT_TURNS=2000
# OFFICETOOL_MAX_ATTACHMENT_CHARS=1000000
# OFFICETOOL_MAX_UPLOAD_MB=200
# OFFICETOOL_ALLOWED_COMMANDS=pwd,ls,cat,rg,head,tail,wc,find,echo,date,python3,git,npm,node,pytest,sed,awk,mkdir,touch,cp,mv

# Token cost display uses built-in OpenAI official pricing table (USD per 1M tokens).
# For custom gateway pricing, adjust app/pricing.py.

# File access scope
# Default extra allowed root already includes ~/Desktop/workbench
# OFFICETOOL_EXTRA_ALLOWED_ROOTS=/Users/<YOU>/Desktop/workbench;/Users/<YOU>/Desktop
# OFFICETOOL_ALLOW_ANY_PATH=false
# OFFCIATOOL_ALLOW_ANY_PATH=false

# Web fetch tool
# Leave blank to allow all domains (no restriction)
# OFFICETOOL_WEB_ALLOWED_DOMAINS=
# OFFICETOOL_WEB_FETCH_TIMEOUT_SEC=12
# OFFICETOOL_WEB_FETCH_MAX_CHARS=120000
# OFFICETOOL_WEB_CA_CERT_PATH=/absolute/path/to/corp-root-ca.cer
# OFFICETOOL_WEB_SKIP_TLS_VERIFY=false

# Note: legacy OFFCIATOOL_* env names are still supported for backward compatibility.
